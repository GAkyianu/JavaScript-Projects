    // A function that uses the += operator
    function myFunction() {
    var sentence = "An apple a day keeps the doctor away"; // creating the variable
    sentence += " is a popular adage"; // Using the += operator to add the variable
    document.getElementById("Concatenate").innerHTML = sentence; // Using the document.getElementById method()
    }
